<div data-extended-modal-target="container" data-action="turbo:submit-end->extended-modal#handleSuccess"
     class="fixed inset-0 flex items-center justify-center hidden overflow-y-auto animated fadeIn"
     style="z-index: 9999;">
  <div class="w-full max-w-lg max-h-screen modalcontent">
    <div class="m-1 bg-white rounded shadow">
      <div class="px-4 py-5 border-b border-gray-200 sm:px-6">
        <i data-action="click->extended-modal#close" class="z-50 cursor-pointer modal-close fas fa-times"></i>
        <h3 class="pt-4 mb-8 text-4xl font-bold text-center heading">
          <%= t('thanks_for_interest') %>
        </h3>
        <p class="text-center"> <%= t('ask_for_info') %> </p>
      </div>
      <form id="contact_form"></form>
    </div>
  </div>
</div>