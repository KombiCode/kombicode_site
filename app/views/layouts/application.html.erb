<!DOCTYPE html>
<html class="antialiased">
  <%= render "shared/head" %>

  <body class="font-sans font-normal leading-normal text-gray-800 bg-white bodycontent debug-screens">
    <turbo-frame id="flash_notice">
      <%= render "shared/flash_notice" %>
    </turbo-frame>
    <%= render "shared/header" %>

    <main class="h-full p-6">
      <%= content_for?(:content) ? yield(:content) : yield %>
    </main>
    <%= render "shared/footer" %>
  </body>
</html>
